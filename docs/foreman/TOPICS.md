# 📌 Foreman's Topics — the triaged work queue

One topic per work stream. The Eisenhower matrix (Do / Schedule /
Delegate / Defer) is applied to the punch list here; the punch list
(`docs/wiki/Shop-Punch-List.md`) stays the ledger of record, this page
is the foreman's working order. Cycles are SESSION-ordered, not
calendar-dated — the foreman works when the shop is open, so "Cycle 1"
means "first thing after the punch-list walk, next session."

```text
EISENHOWER TRIAGE of punch items (2026-07-25)

🟢 DO — done this cycle:
    #3  Copy-out menu on read-only glossary view ............ EXECUTED

🟡 SCHEDULE — the foreman's queue, in order:
    CYCLE 1: #5 instrument-the-decline sweep (red-plum class 2:
             every silent return in _ftb_action_* chains gets a
             breadcrumb + status line)
    CYCLE 2: #4 ghost-tab sweep (legacy save/remove handlers get
             explicit _study_win checks; retire the lucky-TclError
             survival documented in Blueprint §11)
    CYCLE 3: #9 implementation — extend the assistant's grounding
             feed per the proprietor's scope call (BLOCKED ON DELEGATE)
    CYCLE 4: v1.0 release cut — tag + changelog roll-up
             (BLOCKED ON the proprietor's word)

🔵 DELEGATE — with Falcon One (only he can decide):
    #8  E: drive verification; verdict on 7 unmerged child branches
    #9  SCOPE CALL: which sections should the AI assistant read?
        (today: Library files, study notes, glossary, journal, topic
         titles. NOT: Matrix, Planner, commentaries table, topic
         entry bodies)

⚪ DEFER — parked until after v1.0; scope-first before any code:
    #6  Vault Catalog (archive sets + media index)
    #7  Script-export pipeline for narrated segments
```
